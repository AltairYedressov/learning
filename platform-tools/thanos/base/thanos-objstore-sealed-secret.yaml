apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: thanos-objstore-secret
  namespace: monitoring
spec:
  encryptedData:
    # TODO: Generate with kubeseal:
    #   kubectl create secret generic thanos-objstore-secret \
    #     --namespace monitoring \
    #     --from-file=objstore.yml=objstore-config.yaml \
    #     --dry-run=client -o yaml | \
    #     kubeseal --controller-name=sealed-secrets \
    #              --controller-namespace=sealed-secrets \
    #              --format yaml
    #
    # The plaintext objstore.yml should contain:
    #   type: S3
    #   config:
    #     bucket: 372517046622-thanos-dev
    #     endpoint: s3.us-east-1.amazonaws.com
    #     region: us-east-1
    objstore.yml: <SEALED_VALUE>
  template:
    metadata:
      name: thanos-objstore-secret
      namespace: monitoring
    type: Opaque
