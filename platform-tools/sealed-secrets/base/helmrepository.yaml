apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: sealed-secrets
  namespace: flux-system
spec:
  interval: 24h
  url: https://github.com/bitnami-labs/sealed-secrets/tree/main/helm/sealed-secrets
  # Pin to a digest once you've verified the chart
  # digest: sha256:<chart-index-digest>