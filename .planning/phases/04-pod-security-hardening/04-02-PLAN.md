---
phase: 04-pod-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml
  - platform-tools/efk-logging/base/helmrelease-kibana.yaml
autonomous: true
requirements:
  - EKS-03
user_setup: []

must_haves:
  truths:
    - "Elasticsearch pods run as non-root with read-only root filesystem"
    - "Kibana pods run as non-root with read-only root filesystem"
    - "Both EFK pods drop ALL Linux capabilities and disallow privilege escalation"
    - "Both EFK pods have /tmp emptyDir for writable temp directory"
  artifacts:
    - path: "platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml"
      provides: "Elasticsearch HelmRelease with complete security context"
      contains: "runAsNonRoot: true"
    - path: "platform-tools/efk-logging/base/helmrelease-kibana.yaml"
      provides: "Kibana HelmRelease with complete security context"
      contains: "runAsNonRoot: true"
  key_links:
    - from: "platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml"
      to: "Elastic Helm chart upstream"
      via: "podSecurityContext and securityContext value keys"
      pattern: "runAsNonRoot"
    - from: "platform-tools/efk-logging/base/helmrelease-kibana.yaml"
      to: "Elastic Helm chart upstream"
      via: "podSecurityContext and securityContext value keys"
      pattern: "runAsNonRoot"
---

<objective>
Close security context gaps in EFK Elasticsearch and Kibana HelmReleases by adding missing runAsNonRoot and readOnlyRootFilesystem fields plus /tmp emptyDir volumes.

Purpose: Satisfies EKS-03 for platform tool pods that currently have partial security contexts (missing runAsNonRoot and readOnlyRootFilesystem). EFK keeps UID 1000 (upstream default) per established pattern.
Output: Modified HelmRelease files with complete security contexts matching the cluster's hardened standard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pod-security-hardening/04-CONTEXT.md
@.planning/phases/04-pod-security-hardening/04-RESEARCH.md

<interfaces>
<!-- Current Elasticsearch security context (partial -- missing runAsNonRoot, readOnlyRootFilesystem) -->
From platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml lines 78-86:
```yaml
    podSecurityContext:
      runAsUser: 1000
      fsGroup: 1000

    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
```

<!-- Current Kibana security context (partial -- same gaps) -->
From platform-tools/efk-logging/base/helmrelease-kibana.yaml lines 54-62:
```yaml
    podSecurityContext:
      runAsUser: 1000
      fsGroup: 1000

    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
```

<!-- Elastic Helm chart uses these value keys (NOT containerSecurityContext): -->
- podSecurityContext -> pod-level spec.securityContext
- securityContext -> container-level securityContext
- extraVolumes -> additional volumes
- extraVolumeMounts -> additional volumeMounts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing security context fields to Elasticsearch HelmRelease</name>
  <files>platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml</files>
  <read_first>
    - platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml
    - platform-tools/sealed-secrets/base/helmrelease.yaml (lines 111-127 for reference pattern)
    - .planning/phases/04-pod-security-hardening/04-RESEARCH.md (Elasticsearch HelmRelease Values section)
  </read_first>
  <action>
Modify `platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml` values section. Replace the existing `podSecurityContext` and `securityContext` blocks and add `extraVolumes`/`extraVolumeMounts`.

**Replace** the existing `podSecurityContext:` block (lines 78-80) with:
```yaml
    podSecurityContext:
      runAsUser: 1000
      runAsNonRoot: true
      fsGroup: 1000
```
(Added `runAsNonRoot: true` -- keep UID 1000 which is the upstream Elasticsearch default)

**Replace** the existing `securityContext:` block (lines 82-86) with:
```yaml
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      runAsNonRoot: true
      capabilities:
        drop:
          - ALL
```
(Added `readOnlyRootFilesystem: true` and `runAsNonRoot: true`)

**Add** after the `securityContext` block (before `antiAffinity:`):
```yaml
    extraVolumes:
      - name: tmp
        emptyDir:
          sizeLimit: 100Mi

    extraVolumeMounts:
      - name: tmp
        mountPath: /tmp
```

All other values (replicas, resources, volumeClaimTemplate, esJavaOpts, protocol, createCert, secret, extraEnvs, esConfig, antiAffinity, service, lifecycle) stay unchanged.
  </action>
  <verify>
    <automated>cd /Users/altairyedressov/School/finale/learning && grep -q "runAsNonRoot: true" platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml && grep -q "readOnlyRootFilesystem: true" platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml && grep -q "extraVolumes:" platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml && grep -q "extraVolumeMounts:" platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml && grep -q "mountPath: /tmp" platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml && grep -q "sizeLimit: 100Mi" platform-tools/efk-logging/base/helmrelease-elasticsearch.yaml && echo "PASS: Elasticsearch HelmRelease has complete security context"</automated>
  </verify>
  <acceptance_criteria>
    - helmrelease-elasticsearch.yaml contains `runAsNonRoot: true` (appears at least twice -- once in podSecurityContext, once in securityContext)
    - helmrelease-elasticsearch.yaml contains `readOnlyRootFilesystem: true`
    - helmrelease-elasticsearch.yaml contains `runAsUser: 1000` (kept from upstream default, NOT 1001)
    - helmrelease-elasticsearch.yaml contains `extraVolumes:` with `name: tmp` and `emptyDir:` with `sizeLimit: 100Mi`
    - helmrelease-elasticsearch.yaml contains `extraVolumeMounts:` with `name: tmp` and `mountPath: /tmp`
    - helmrelease-elasticsearch.yaml still contains `allowPrivilegeEscalation: false` and `drop:` / `- ALL`
    - helmrelease-elasticsearch.yaml still contains all original values (replicas: 2, esJavaOpts, protocol: http, etc.)
  </acceptance_criteria>
  <done>Elasticsearch HelmRelease has runAsNonRoot: true in both pod and container security contexts, readOnlyRootFilesystem: true, and /tmp emptyDir with 100Mi sizeLimit. UID stays 1000 (upstream default).</done>
</task>

<task type="auto">
  <name>Task 2: Add missing security context fields to Kibana HelmRelease</name>
  <files>platform-tools/efk-logging/base/helmrelease-kibana.yaml</files>
  <read_first>
    - platform-tools/efk-logging/base/helmrelease-kibana.yaml
    - platform-tools/sealed-secrets/base/helmrelease.yaml (lines 111-127 for reference pattern)
    - .planning/phases/04-pod-security-hardening/04-RESEARCH.md (Kibana HelmRelease Values section)
  </read_first>
  <action>
Modify `platform-tools/efk-logging/base/helmrelease-kibana.yaml` values section. Replace the existing `podSecurityContext` and `securityContext` blocks and add `extraVolumes`/`extraVolumeMounts`.

**Replace** the existing `podSecurityContext:` block (lines 54-56) with:
```yaml
    podSecurityContext:
      runAsUser: 1000
      runAsNonRoot: true
      fsGroup: 1000
```
(Added `runAsNonRoot: true` -- keep UID 1000 which is the upstream Kibana default)

**Replace** the existing `securityContext:` block (lines 58-62) with:
```yaml
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      runAsNonRoot: true
      capabilities:
        drop:
          - ALL
```
(Added `readOnlyRootFilesystem: true` and `runAsNonRoot: true`)

**Add** after the `securityContext` block (before `service:`):
```yaml
    extraVolumes:
      - name: tmp
        emptyDir:
          sizeLimit: 100Mi

    extraVolumeMounts:
      - name: tmp
        mountPath: /tmp
```

All other values (replicas, protocol, elasticsearchHosts, resources, healthCheckPath, service, kibanaConfig) stay unchanged.
  </action>
  <verify>
    <automated>cd /Users/altairyedressov/School/finale/learning && grep -q "runAsNonRoot: true" platform-tools/efk-logging/base/helmrelease-kibana.yaml && grep -q "readOnlyRootFilesystem: true" platform-tools/efk-logging/base/helmrelease-kibana.yaml && grep -q "extraVolumes:" platform-tools/efk-logging/base/helmrelease-kibana.yaml && grep -q "extraVolumeMounts:" platform-tools/efk-logging/base/helmrelease-kibana.yaml && grep -q "mountPath: /tmp" platform-tools/efk-logging/base/helmrelease-kibana.yaml && grep -q "sizeLimit: 100Mi" platform-tools/efk-logging/base/helmrelease-kibana.yaml && echo "PASS: Kibana HelmRelease has complete security context"</automated>
  </verify>
  <acceptance_criteria>
    - helmrelease-kibana.yaml contains `runAsNonRoot: true` (appears at least twice -- once in podSecurityContext, once in securityContext)
    - helmrelease-kibana.yaml contains `readOnlyRootFilesystem: true`
    - helmrelease-kibana.yaml contains `runAsUser: 1000` (kept from upstream default, NOT 1001)
    - helmrelease-kibana.yaml contains `extraVolumes:` with `name: tmp` and `emptyDir:` with `sizeLimit: 100Mi`
    - helmrelease-kibana.yaml contains `extraVolumeMounts:` with `name: tmp` and `mountPath: /tmp`
    - helmrelease-kibana.yaml still contains `allowPrivilegeEscalation: false` and `drop:` / `- ALL`
    - helmrelease-kibana.yaml still contains all original values (replicas: 1, elasticsearchHosts, healthCheckPath, kibanaConfig, etc.)
  </acceptance_criteria>
  <done>Kibana HelmRelease has runAsNonRoot: true in both pod and container security contexts, readOnlyRootFilesystem: true, and /tmp emptyDir with 100Mi sizeLimit. UID stays 1000 (upstream default).</done>
</task>

</tasks>

<verification>
1. Elasticsearch HelmRelease has runAsNonRoot: true in podSecurityContext and securityContext
2. Elasticsearch HelmRelease has readOnlyRootFilesystem: true
3. Elasticsearch HelmRelease has extraVolumes/extraVolumeMounts for /tmp with sizeLimit 100Mi
4. Kibana HelmRelease has runAsNonRoot: true in podSecurityContext and securityContext
5. Kibana HelmRelease has readOnlyRootFilesystem: true
6. Kibana HelmRelease has extraVolumes/extraVolumeMounts for /tmp with sizeLimit 100Mi
7. Both files still valid YAML (no syntax errors introduced)
8. Both files retain all original non-security values unchanged
</verification>

<success_criteria>
- grep confirms runAsNonRoot: true in both EFK HelmReleases
- grep confirms readOnlyRootFilesystem: true in both EFK HelmReleases
- grep confirms extraVolumes and extraVolumeMounts in both EFK HelmReleases
- Both files retain UID 1000 (not 1001 -- upstream default preserved)
- All non-security values unchanged (replicas, resources, config, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/04-pod-security-hardening/04-02-SUMMARY.md`
</output>
