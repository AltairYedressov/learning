---
phase: 07-iam-rbac-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform-tools/sealed-secrets/base/RBAC.yaml
autonomous: true
requirements:
  - IAM-01
  - IAM-02
must_haves:
  truths:
    - "All ClusterRoleBindings are documented with justification"
    - "No unnecessary system:masters bindings exist in git-managed manifests"
    - "Each IRSA custom policy has been reviewed and findings documented"
    - "Sealed-secrets RBAC.yaml has inline comments explaining each binding"
    - "EKS access entries are documented with justification"
  artifacts:
    - path: "platform-tools/sealed-secrets/base/RBAC.yaml"
      provides: "RBAC with inline justification comments"
      contains: "Purpose:"
    - path: ".planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md"
      provides: "RBAC audit report with CRB inventory, IRSA review, access entries"
      contains: "ClusterRoleBinding"
  key_links:
    - from: "platform-tools/sealed-secrets/base/RBAC.yaml"
      to: "Kubernetes RBAC"
      via: "Flux GitOps reconciliation"
      pattern: "sealed-secrets-reader"
---

<objective>
Audit all Kubernetes RBAC ClusterRoleBindings, review IRSA custom policy scoping, document EKS access entries, and add inline justification comments to sealed-secrets RBAC manifest.

Purpose: Satisfies IAM-01 (RBAC least-privilege audit) and IAM-02 (IRSA verification). Produces a comprehensive audit trail documenting why each privileged binding and policy exists, confirming no unnecessary system:masters bindings and that IRSA roles have minimal required permissions.
Output: Enhanced sealed-secrets RBAC.yaml with inline comments + phase summary serving as the audit report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-iam-rbac-hardening/07-RESEARCH.md

@platform-tools/sealed-secrets/base/RBAC.yaml
@terraform-infra/iam-role-module/Policies/karpenter_policy.json
@terraform-infra/iam-role-module/Policies/velero_policy.json
@terraform-infra/iam-role-module/Policies/aws_lb_controller_policy.json
@terraform-infra/iam-role-module/Policies/thanos_policy.json
@terraform-infra/eks-cluster/access-entries.tf
@terraform-infra/root/dev/iam-roles/main.tf
@clusters/dev-projectx/flux-system/gotk-components.yaml

<interfaces>
<!-- Current sealed-secrets RBAC.yaml has a ClusterRole (sealed-secrets-reader) and ClusterRoleBinding (sealed-secrets-reader-binding). The ClusterRole grants get/list/watch on bitnami.com sealedsecrets. The binding maps it to group platform-ops. Existing comments are good but need security justification additions per D-12. -->

<!-- IRSA Policy Review Findings from RESEARCH.md (use these for audit report): -->

Karpenter (karpenter_policy.json): Well-scoped. EC2 launch/fleet ops, IAM PassRole scoped to specific ARN, EKS DescribeCluster on specific cluster, SQS scoped to specific queue. Flag: iam:CreateInstanceProfile on Resource:"*" -- required by Karpenter for dynamic instance profile management.

Velero (velero_policy.json): Well-scoped. S3 scoped to specific bucket (372517046622-velero-backups-dev). EC2 snapshot actions require Resource:"*" per AWS docs.

AWS LB Controller (aws_lb_controller_policy.json): Matches official AWS-recommended policy. Flag: waf-regional:* and shield:* unused but standard inclusion.

Thanos (thanos_policy.json): Minimal. Only S3 ops scoped to specific bucket (372517046622-thanos-dev).

<!-- RBAC Inventory from RESEARCH.md (use for audit report): -->

Git-managed CRBs:
- cluster-reconciler-flux-system: cluster-admin for kustomize-controller (flux-system)
- crd-controller-flux-system: custom crd-controller role for Flux controllers
- sealed-secrets-reader-binding: custom read-only for platform-ops group

EKS Access Entries:
- altoha (IAM user): AmazonEKSClusterAdminPolicy (break-glass admin, D-07)
- GithubActionsTerraformDeploy (IAM role): AmazonEKSClusterAdminPolicy (CI/CD, D-06)
- eks_worker_nodes_role: EC2_LINUX (node auth, no policy)

No system:masters bindings in git-managed manifests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance sealed-secrets RBAC.yaml with inline justification comments</name>
  <files>platform-tools/sealed-secrets/base/RBAC.yaml</files>
  <read_first>
    platform-tools/sealed-secrets/base/RBAC.yaml
  </read_first>
  <action>
Per D-12, add inline YAML comments explaining WHY each binding exists. The file already has good comments; enhance with explicit security justification per IAM-01 audit requirements.

The file contains two YAML documents separated by a `---` line (ClusterRole and ClusterRoleBinding). Keep both documents intact. Make ONLY comment changes:

BEFORE the ClusterRole (replace the existing 3-line comment block at the top), add:
```
# -- Sealed Secrets Reader RBAC -----------------------------------------------
# Purpose: Allow platform-ops group to inspect SealedSecret custom resources
# without granting access to decrypted Secret objects.
# The controller's own RBAC (created by Helm chart) handles encryption/decryption.
#
# Security justification (Phase 7 IAM-01 audit):
#   - ClusterRole grants GET/LIST/WATCH on SealedSecrets CRs only
#   - No access to core "secrets" API -- prevents reading decrypted values
#   - Bound to "platform-ops" group -- scoped to authenticated ops members
#   - Read-only verbs only -- no create/update/delete/patch
# --------------------------------------------------------------------------
```

BEFORE the ClusterRoleBinding (between the `---` separator and the `apiVersion` line), add:
```
# Binding: sealed-secrets-reader-binding
# Justification: Maps sealed-secrets-reader ClusterRole to platform-ops group.
# This is the only custom (non-Helm) ClusterRoleBinding in the cluster.
# Grants read-only SealedSecret CR access -- no privilege escalation risk.
```

Keep the existing inline comment on the `rules` section ("Do NOT grant secrets verbs here...") and the comment on `name: platform-ops` ("# map to your SSO group").

Do NOT change any functional YAML (apiVersion, kind, metadata, rules, roleRef, subjects). Only add/enhance comments.
  </action>
  <verify>
    <automated>cd /Users/altairyedressov/School/finale/learning && grep -c "Security justification" platform-tools/sealed-secrets/base/RBAC.yaml && grep -c "Justification:" platform-tools/sealed-secrets/base/RBAC.yaml && grep "sealed-secrets-reader-binding" platform-tools/sealed-secrets/base/RBAC.yaml</automated>
  </verify>
  <acceptance_criteria>
    - platform-tools/sealed-secrets/base/RBAC.yaml contains "Security justification (Phase 7 IAM-01 audit):"
    - platform-tools/sealed-secrets/base/RBAC.yaml contains "Justification: Maps sealed-secrets-reader ClusterRole to platform-ops group"
    - platform-tools/sealed-secrets/base/RBAC.yaml contains "Purpose: Allow platform-ops group to inspect SealedSecret"
    - platform-tools/sealed-secrets/base/RBAC.yaml still contains "kind: ClusterRole" and "kind: ClusterRoleBinding"
    - platform-tools/sealed-secrets/base/RBAC.yaml still contains "name: sealed-secrets-reader"
    - platform-tools/sealed-secrets/base/RBAC.yaml still contains 'verbs: ["get", "list", "watch"]'
    - platform-tools/sealed-secrets/base/RBAC.yaml still contains "name: platform-ops"
  </acceptance_criteria>
  <done>Sealed-secrets RBAC.yaml has inline comments explaining why the ClusterRole and ClusterRoleBinding exist, per D-12. Functional YAML unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Produce RBAC and IRSA audit report in phase summary</name>
  <files>.planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md</files>
  <read_first>
    terraform-infra/iam-role-module/Policies/karpenter_policy.json,
    terraform-infra/iam-role-module/Policies/velero_policy.json,
    terraform-infra/iam-role-module/Policies/aws_lb_controller_policy.json,
    terraform-infra/iam-role-module/Policies/thanos_policy.json,
    terraform-infra/eks-cluster/access-entries.tf,
    terraform-infra/root/dev/iam-roles/main.tf,
    clusters/dev-projectx/flux-system/gotk-components.yaml,
    platform-tools/sealed-secrets/base/RBAC.yaml
  </read_first>
  <action>
Create the plan summary file that doubles as the RBAC/IRSA audit report. The summary MUST include these sections (follow the standard summary template structure but add audit-specific content):

**RBAC ClusterRoleBinding Inventory (IAM-01):**

Include a table with ALL git-managed ClusterRoleBindings found in the codebase:

| Name | ClusterRole | Subjects | Source | Justification |
|------|-------------|----------|--------|---------------|
| cluster-reconciler-flux-system | cluster-admin | SA: kustomize-controller (flux-system) | gotk-components.yaml | Flux kustomize-controller needs full cluster access to reconcile any resource type across all namespaces. Required for GitOps operation. Accepted per D-14/D-15 |
| crd-controller-flux-system | crd-controller-flux-system (custom) | SA: helm-controller, kustomize-controller, notification-controller, source-controller (flux-system) | gotk-components.yaml | Flux controllers need CRD management. Custom role (not cluster-admin) |
| sealed-secrets-reader-binding | sealed-secrets-reader (custom) | Group: platform-ops | RBAC.yaml | Read-only SealedSecret CR inspection. Cannot read decrypted Secrets. Enhanced with inline comments per D-12 |

Note per D-13: Helm-managed CRBs (sealed-secrets controller, Kyverno, Karpenter, AWS LB Controller) are created at runtime by their Helm charts and documented by category -- they follow standard patterns for their respective tools.

**system:masters Assessment:** Confirm no custom system:masters bindings in git. EKS access entries use AmazonEKSClusterAdminPolicy (EKS-native, not system:masters). Flux uses cluster-admin ClusterRole (not system:masters group).

**IRSA Custom Policy Review (IAM-02):**

Include a table per D-09:

| IRSA Role | Policy File | Assessment | Flags |
|-----------|-------------|------------|-------|
| karpenter_irsa_role | karpenter_policy.json | Well-scoped. EC2 launch ops, IAM PassRole to specific ARN, EKS DescribeCluster on specific cluster, SQS to specific queue | iam:CreateInstanceProfile on Resource:"*" -- required by Karpenter for dynamic instance profiles |
| velero-role | velero_policy.json | Well-scoped. S3 scoped to 372517046622-velero-backups-dev bucket. EC2 snapshot actions require Resource:"*" | None |
| aws-lb-controller-role | aws_lb_controller_policy.json | Matches official AWS-recommended policy. Mutating ops conditioned on elbv2.k8s.aws/cluster tag | waf-regional:* and shield:* unused but standard canonical inclusion |
| thanos-role | thanos_policy.json | Minimal. S3 ops scoped to 372517046622-thanos-dev bucket only | None |
| ebs-csi-irsa-role | AmazonEBSCSIDriverPolicy (AWS-managed) | Skipped per D-10 -- AWS-managed, already scoped | N/A |

Note per D-11: No overprivileged IRSA policies found that require modification. All custom policies are well-scoped with resource ARN constraints where applicable. Flagged items (Karpenter instance profiles, LB Controller WAF/Shield) are standard requirements or canonical inclusions -- document only, no changes.

**EKS Access Entries (IAM-01):**

Document per D-06/D-07/D-08:

| Principal | Policy | Justification |
|-----------|--------|---------------|
| altoha (IAM user) | AmazonEKSClusterAdminPolicy | Solo project admin, break-glass access (D-07) |
| GithubActionsTerraformDeploy (IAM role) | AmazonEKSClusterAdminPolicy | CI/CD Terraform apply needs cluster-admin for addons, access entries, Flux bootstrap (D-06) |
| eks_worker_nodes_role | EC2_LINUX (no policy) | Node authentication entry only |

Backup: Terraform state serves as the access entry backup per D-08 -- all entries are fully defined in terraform-infra/eks-cluster/access-entries.tf.

**All IRSA roles have OIDC conditions** scoping trust to specific service accounts (StringEquals on :sub and :aud claims). Verify this by reading the assume_role_conditions in main.tf for each IRSA module.
  </action>
  <verify>
    <automated>cd /Users/altairyedressov/School/finale/learning && test -f .planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md && grep -c "ClusterRoleBinding" .planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md && grep -c "IRSA" .planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md exists
    - File contains "ClusterRoleBinding Inventory" or "ClusterRoleBinding" table
    - File contains "system:masters" assessment
    - File contains "IRSA" review section with all 4 custom policies (karpenter, velero, aws_lb_controller, thanos)
    - File contains "EKS Access Entries" section with altoha and GithubActionsTerraformDeploy
    - File contains "sealed-secrets-reader-binding" in the CRB inventory
    - File contains "cluster-reconciler-flux-system" in the CRB inventory
    - File contains "No overprivileged IRSA policies" or equivalent finding
    - File contains reference to D-14 for Flux cluster-admin justification
  </acceptance_criteria>
  <done>Comprehensive RBAC/IRSA audit report produced. All ClusterRoleBindings documented with justification. All 4 IRSA custom policies reviewed. EKS access entries documented. No unnecessary system:masters bindings confirmed. No IRSA policy changes needed.</done>
</task>

</tasks>

<verification>
- platform-tools/sealed-secrets/base/RBAC.yaml has security justification comments
- RBAC.yaml functional content unchanged (ClusterRole + ClusterRoleBinding)
- Phase summary contains RBAC inventory, IRSA review, access entry documentation
- No system:masters bindings identified in git-managed manifests
- All 4 IRSA custom policies reviewed and documented
</verification>

<success_criteria>
- Sealed-secrets RBAC.yaml enhanced with inline justification comments per D-12
- All git-managed ClusterRoleBindings documented with justification per D-13/D-14
- IRSA policy review complete for all 4 custom policies per D-09
- EKS access entries documented per D-06/D-07/D-08
- No unnecessary system:masters bindings confirmed (IAM-01 satisfied)
- IRSA roles verified as minimal (IAM-02 satisfied)
</success_criteria>

<output>
After completion, create/update `.planning/phases/07-iam-rbac-hardening/07-02-SUMMARY.md`
</output>
