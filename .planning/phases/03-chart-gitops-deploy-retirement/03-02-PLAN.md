---
phase: 03-chart-gitops-deploy-retirement
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - HelmCharts/portfolio/templates/02-frontend.yaml
autonomous: true
requirements: [DEP-01, DEP-05, SEC-04]
must_haves:
  truths:
    - "Frontend Deployment + Service renamed from portfolio-frontend to portfolio-web; labels app: portfolio-web"
    - "Container references .Values.images.web, .Values.ports.web (=3000), .Values.resources.web"
    - "Both probes hit GET /health on port 3000 (unchanged path; D-03)"
    - "Env: PORT={{ ports.web }}, BACKEND_URL=http://portfolio-api.portfolio.svc.cluster.local:5000, NODE_ENV=production, API_URL kept for back-compat with server.js"
    - "serviceAccountName: portfolio-web is set (D-10)"
    - "Existing hardened securityContext preserved (SEC-04)"
  artifacts:
    - path: "HelmCharts/portfolio/templates/02-frontend.yaml"
      provides: "portfolio-web Deployment + Service at port 3000 with SA, env, and probes"
      contains: "name: portfolio-web"
  key_links:
    - from: "portfolio-web Deployment"
      to: "portfolio-api Service"
      via: "env BACKEND_URL / API_URL"
      pattern: "portfolio-api.portfolio.svc.cluster.local:5000"
    - from: "portfolio-web Deployment"
      to: "ServiceAccount portfolio-web"
      via: "spec.template.spec.serviceAccountName"
      pattern: "serviceAccountName:\\s*portfolio-web"
---

<objective>
Rename the frontend Kubernetes resources from `portfolio-frontend` to `portfolio-web` (D-02, D-07), re-key template references to `.Values.{images,ports,replicas,resources}.web`, bind to the dedicated `portfolio-web` ServiceAccount, and ensure the env block matches what the new frontend container expects (Phase 1 server.js reads both `PORT` and `API_URL`; Phase 3 also wires `BACKEND_URL` for completeness per D-04).

Purpose: The chart's frontend resource name must match the new image and value-key rename so VirtualService/NetworkPolicy/AuthorizationPolicy selectors (Plan 03-03) can point at `app: portfolio-web`.

Output: Updated 02-frontend.yaml.

Honors D-01 (port 5000 target), D-02 (rename), D-03 (/health probe — path unchanged), D-04 (env), D-09 (resources unchanged), D-10 (SA).

Depends on Plan 03-01 only for values.yaml keys (`images.web`, `ports.web`, `resources.web`, `serviceAccount.web`) — no shared file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-chart-gitops-deploy-retirement/03-CONTEXT.md
@.planning/phases/03-chart-gitops-deploy-retirement/03-01-PLAN.md
@.planning/phases/01-package-local-verify/01-SUMMARY.md
@HelmCharts/portfolio/templates/02-frontend.yaml
@HelmCharts/portfolio/values.yaml

<interfaces>
values.yaml keys this plan consumes (defined in Plan 03-01):
  .Values.namespace.name
  .Values.replicas.web
  .Values.images.web
  .Values.ports.web (=3000)
  .Values.resources.web.{requests,limits}.{cpu,memory}
  .Values.api.url (="http://portfolio-api.portfolio.svc.cluster.local:5000")
  .Values.serviceAccount.web (="portfolio-web")

Frontend container env consumed (per Phase 1 server.js):
  PORT — which port to listen on
  API_URL — backend URL used by http-proxy-middleware

Frontend /health endpoint (Phase 1): app.get("/health") returns 200 — does NOT proxy to backend (D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HelmCharts/portfolio/templates/02-frontend.yaml with portfolio-web name + SA + env (D-02, D-04, D-07, D-10)</name>
  <files>HelmCharts/portfolio/templates/02-frontend.yaml</files>
  <action>
Replace the full file with the content below. Changes vs. existing file:

- Deployment + Service metadata.name: `portfolio-frontend` → `portfolio-web`
- All labels `app: portfolio-frontend` → `app: portfolio-web`
- Container name: `frontend` → `web`
- Value refs: `.Values.images.frontend` → `.Values.images.web`; `.Values.ports.frontend` → `.Values.ports.web`; `.Values.replicas.frontend` → `.Values.replicas.web`; `.Values.resources.frontend` → `.Values.resources.web`
- Add `serviceAccountName: {{ .Values.serviceAccount.web | default "portfolio-web" }}` (D-10)
- Env block: keep existing `API_URL` and `PORT`, ADD `BACKEND_URL` (D-04) and `NODE_ENV` (production-mode hardening for Express/helmet)
- Probes: already `/health` — unchanged. Now on port 3000 via ports.web (D-03).
- Preserve the hardened securityContext block verbatim (SEC-04).
- Keep the tmp emptyDir mount (readOnlyRootFilesystem needs writable /tmp).

Full file:

```yaml
# ── Frontend (portfolio-web) Deployment ─────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-web
  namespace: {{ .Values.namespace.name }}
  labels:
    app: portfolio-web
    tier: frontend
spec:
  replicas: {{ .Values.replicas.web }}
  selector:
    matchLabels:
      app: portfolio-web
  template:
    metadata:
      labels:
        app: portfolio-web
        tier: frontend
    spec:
      serviceAccountName: {{ .Values.serviceAccount.web | default "portfolio-web" }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: web
          image: {{ .Values.images.web }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: {{ .Values.ports.web }}
          env:
            - name: PORT
              value: {{ .Values.ports.web | quote }}
            - name: API_URL
              value: {{ .Values.api.url | quote }}
            - name: BACKEND_URL
              value: {{ .Values.api.url | quote }}
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              cpu: {{ .Values.resources.web.requests.cpu }}
              memory: {{ .Values.resources.web.requests.memory }}
            limits:
              cpu: {{ .Values.resources.web.limits.cpu }}
              memory: {{ .Values.resources.web.limits.memory }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.ports.web }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.ports.web }}
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 100Mi
---
# ── Frontend (portfolio-web) Service — ClusterIP, Istio handles ingress ─────
apiVersion: v1
kind: Service
metadata:
  name: portfolio-web
  namespace: {{ .Values.namespace.name }}
  labels:
    app: portfolio-web
spec:
  type: ClusterIP
  selector:
    app: portfolio-web
  ports:
    - port: {{ .Values.ports.web }}
      targetPort: {{ .Values.ports.web }}
      protocol: TCP
```

Why BOTH `API_URL` and `BACKEND_URL`: Phase 1 server.js reads `API_URL` (default `http://portfolio-api:5000`). D-04 names `BACKEND_URL` explicitly. Keeping both future-proofs any later refactor without breaking the running app.
  </action>
  <verify>
    <automated>helm lint HelmCharts/portfolio && helm template HelmCharts/portfolio | grep -q 'name: portfolio-web' && helm template HelmCharts/portfolio | grep -q 'app: portfolio-web' && helm template HelmCharts/portfolio | grep -q 'serviceAccountName: portfolio-web' && helm template HelmCharts/portfolio | grep -q 'containerPort: 3000' && helm template HelmCharts/portfolio | grep -q 'portfolio-api.portfolio.svc.cluster.local:5000' && ! helm template HelmCharts/portfolio | grep -q 'portfolio-frontend'</automated>
  </verify>
  <done>helm template renders a `portfolio-web` Deployment + Service on port 3000, with SA portfolio-web, BACKEND_URL/API_URL pointing at portfolio-api:5000, and no remaining `portfolio-frontend` references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| istio-ingress → portfolio-web | Untrusted → mesh-internal (Plan 03-03 AuthorizationPolicy + NetworkPolicy enforce) |
| portfolio-web → portfolio-api | Mesh pod-to-pod with mTLS; locked by Plan 03-03 AuthorizationPolicy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | portfolio-web container fs | mitigate | readOnlyRootFilesystem + tmp emptyDir; dropped caps; non-root |
| T-03-08 | Spoofing | portfolio-web identity | mitigate | Dedicated SA portfolio-web for distinct SPIFFE principal |
| T-03-09 | Information Disclosure | NODE_ENV=development verbose errors | mitigate | NODE_ENV pinned to "production" in env |
| T-03-10 | Denial of Service | /health probe | accept | Cheap static 200 (Phase 1 implementation) |
</threat_model>

<verification>
- `helm lint` passes.
- `helm template HelmCharts/portfolio | grep -c 'app: portfolio-web'` ≥ 4 (Deployment metadata, selector.matchLabels, template.labels, Service.selector + metadata).
- Zero remaining occurrences of `portfolio-frontend` anywhere under `HelmCharts/portfolio/`.
- Rendered Service listens on port 3000.
</verification>

<success_criteria>
Maps to ROADMAP Phase 3 success criteria #1 (probes, resources, PSS-Restricted securityContext for the web pod). Requirements: DEP-01, DEP-05, SEC-04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-gitops-deploy-retirement/03-02-SUMMARY.md`.
</output>
