---
id: 260415-q8z
type: quick
slug: add-terminal-typing-asmr-sound-synced-to
files_modified:
  - app/portfolio/frontend/public/index.html
  - app/portfolio/frontend/public/js/main.js
  - app/portfolio/frontend/public/css/style.css
  - app/portfolio/frontend/public/audio/keypress.mp3
  - app/portfolio/frontend/server.js
autonomous: true
must_haves:
  truths:
    - "A sound toggle button appears in the top-right nav, adjacent to the theme toggle, with matching styling."
    - "Sound preference persists across reloads via localStorage key 'sound-enabled', defaulting to OFF."
    - "When sound is ON, each character typed in the skills and experience terminal typewriter animations emits a keypress sound."
    - "First click on the toggle counts as user gesture and successfully unlocks the audio (no autoplay-blocked warning on subsequent playback)."
    - "When sound is OFF (default), no audio plays and no autoplay warnings are emitted to the console."
  artifacts:
    - path: "app/portfolio/frontend/public/audio/keypress.mp3"
      provides: "Royalty-free mechanical keypress audio asset (< 50KB, short < 150ms clip)."
    - path: "app/portfolio/frontend/public/js/main.js"
      provides: "initSound() init, playKey() throttled emitter, integration into typeCmd() for both terminals."
    - path: "app/portfolio/frontend/public/index.html"
      provides: "#soundToggle button next to #themeToggle in <nav>, with sound-on/sound-off SVG icons."
    - path: "app/portfolio/frontend/public/css/style.css"
      provides: ".nav__sound rules mirroring .nav__theme styling idiom, including light/dark icon visibility swap."
    - path: "app/portfolio/frontend/server.js"
      provides: "CSP mediaSrc directive allowing 'self' so the audio asset is permitted under helmet CSP."
  key_links:
    - from: "index.html #soundToggle"
      to: "main.js initSound()"
      via: "click listener toggles localStorage + DOM class"
      pattern: "getElementById\\(\"soundToggle\"\\)"
    - from: "main.js typeCmd tick()"
      to: "playKey()"
      via: "called once per character when soundEnabled && audioUnlocked"
      pattern: "playKey\\("
    - from: "server.js helmet CSP"
      to: "/audio/keypress.mp3"
      via: "mediaSrc: ['self'] directive"
      pattern: "mediaSrc"
---

<objective>
Add a terminal-typing ASMR sound effect synced to the existing typewriter animations (skills + experience terminals) in the portfolio frontend, plus a sound on/off toggle button in the top-right nav next to the existing theme toggle. Preference persists in localStorage ("sound-enabled") and defaults OFF to respect the browser autoplay policy; the first click on the toggle is the user gesture that unlocks audio playback.

Purpose: UX polish — make the signature terminal typewriter animation more immersive while respecting user control.
Output: Working sound toggle button with persisted state, bundled royalty-free keypress audio asset, and sound integrated into both typewriter loops.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

@app/portfolio/frontend/public/index.html
@app/portfolio/frontend/public/js/main.js
@app/portfolio/frontend/public/css/style.css
@app/portfolio/frontend/server.js

<interfaces>
Key anchor points extracted from the codebase (frontend is a single-page static site under app/portfolio/frontend/public; server.js mounts it via express.static):

index.html:17-32 — top-right nav with #themeToggle button and #burger button. New #soundToggle button goes between #themeToggle and #burger, using the same structural idiom (two SVG icons swapped by a class).

main.js:1-15 — DOMContentLoaded bootstrap block calls initTheme(), navScroll(), etc. Add initSound() alongside initTheme().

main.js:17-39 — initTheme() pattern to mirror:
  - reads localStorage key, toggles a root attribute/class
  - addEventListener("click") on the button swaps state and writes localStorage

main.js:308-317 — typeCmd() in the skills terminal (inside runSkillsSequence scope). Per-char tick at setTimeout(35 + random*20). Hook playKey() here.

main.js ~433-470 — second typeCmd() inside the experience terminal (expTyping). Identical structure. Hook playKey() here too.

style.css:247-264 — .nav__theme styling idiom: 36x36 circle, border, icon swap via [data-theme="light"] .nav__theme-icon--*. New .nav__sound rules mirror this but swap icons via a parent class (e.g., [data-sound="on"] .nav__sound-icon--off { display: none; }).

server.js:19-32 — helmet CSP config. Currently has scriptSrc, styleSrc, fontSrc, imgSrc, connectSrc but NO mediaSrc. Audio will be blocked without it. Add mediaSrc: ["'self'"].

server.js:60-65 — express.static serves everything under public/. New public/audio/keypress.mp3 is auto-served at /audio/keypress.mp3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keypress audio asset + CSP mediaSrc</name>
  <files>
    app/portfolio/frontend/public/audio/keypress.mp3
    app/portfolio/frontend/server.js
  </files>
  <action>
    1. Create directory app/portfolio/frontend/public/audio/.
    2. Source a royalty-free, CC0-licensed mechanical keypress sample (< 150ms, < 50KB, mono, ~44.1kHz MP3). Acceptable sources: freesound.org CC0 packs, Mixkit free SFX, or Pixabay free sounds. Download with curl into the audio directory as keypress.mp3. If the chosen asset is WAV, convert to MP3 via `ffmpeg -i in.wav -codec:a libmp3lame -qscale:a 5 keypress.mp3` (smaller than WAV, broad browser support). Record the source URL + license in a sibling file `app/portfolio/frontend/public/audio/README.md` (one-liner citing source, license, and original filename).
    3. Edit server.js helmet CSP directives (around line 22-29): add `mediaSrc: ["'self'"]` to the directives object so the audio asset is not blocked by CSP.
  </action>
  <verify>
    <automated>test -f app/portfolio/frontend/public/audio/keypress.mp3 && test -f app/portfolio/frontend/public/audio/README.md && grep -q "mediaSrc" app/portfolio/frontend/server.js</automated>
  </verify>
  <done>Audio file exists, is &lt; 50KB, README.md documents source + license, CSP includes mediaSrc 'self'.</done>
</task>

<task type="auto">
  <name>Task 2: Add sound toggle button (HTML + CSS)</name>
  <files>
    app/portfolio/frontend/public/index.html
    app/portfolio/frontend/public/css/style.css
  </files>
  <action>
    1. In index.html, inside `<nav class="nav" id="nav">` (around line 17-32), insert a new `<button class="nav__sound" id="soundToggle" aria-label="Toggle sound" aria-pressed="false">` element immediately AFTER the existing `<button class="nav__theme" id="themeToggle">…</button>` and BEFORE `<button class="nav__burger">`. The button contains two inline SVGs mirroring the theme-toggle pattern:
       - `<svg class="nav__sound-icon nav__sound-icon--on" …>` — a speaker-with-waves icon
       - `<svg class="nav__sound-icon nav__sound-icon--off" …>` — a speaker-muted icon (speaker with X)
       Use 18x18 viewBox, `fill="none" stroke="currentColor" stroke-width="2"`, matching the theme toggle's visual weight. Inline SVG paths — do not reference external icon libs.
    2. Also add `data-sound="off"` attribute to the `<html>` root element (line 2) alongside `data-theme="light"` as the default — JS will flip it to "on" when enabled.
    3. In style.css, immediately AFTER the `.nav__theme` block (after line 264), add `.nav__sound` rules that mirror `.nav__theme` exactly (same 36x36 circle, same border, same hover, same margin/gap as the theme button — inspect the surrounding `.nav` flex layout). Add icon visibility rules:
       - `.nav__sound-icon--on { display: none; }` (default hidden — sound off by default)
       - `[data-sound="on"] .nav__sound-icon--on { display: block; }`
       - `[data-sound="on"] .nav__sound-icon--off { display: none; }`
       The "off" (muted) icon shows by default; "on" icon shows when sound is enabled.
  </action>
  <verify>
    <automated>grep -q 'id="soundToggle"' app/portfolio/frontend/public/index.html && grep -q "nav__sound-icon--on" app/portfolio/frontend/public/index.html && grep -q "data-sound" app/portfolio/frontend/public/index.html && grep -q "\.nav__sound" app/portfolio/frontend/public/css/style.css && grep -q '\[data-sound="on"\]' app/portfolio/frontend/public/css/style.css</automated>
  </verify>
  <done>Sound toggle button renders in nav next to theme toggle with matching styling; icon swaps based on [data-sound="on"] attribute on &lt;html&gt;.</done>
</task>

<task type="auto">
  <name>Task 3: Wire sound state + playback into typewriter loops</name>
  <files>
    app/portfolio/frontend/public/js/main.js
  </files>
  <action>
    1. At the top of main.js (module scope, above the DOMContentLoaded block around line 1), add module-scope state:
       - `let soundEnabled = false;`
       - `let audioUnlocked = false;`
       - `let keyAudio = null;` (lazily created HTMLAudioElement pool)
       - `let lastPlay = 0;` (throttle timestamp)
       - Const `KEY_THROTTLE_MS = 25;` (prevents clipping when typing is fast).
    2. Add `initSound()` function that:
       - Reads `localStorage.getItem("sound-enabled")`. If value is `"true"`, set `soundEnabled = true` and set `document.documentElement.setAttribute("data-sound", "on")`; otherwise leave default off. Default is OFF per autoplay policy requirement.
       - Gets `#soundToggle` button; if missing, return.
       - Sets `aria-pressed` to match current soundEnabled.
       - Attaches click handler that:
         a) Toggles `soundEnabled`.
         b) Writes `localStorage.setItem("sound-enabled", String(soundEnabled))`.
         c) Updates `data-sound` attribute on `<html>` and `aria-pressed` on the button.
         d) On the FIRST click where `soundEnabled` becomes true and `audioUnlocked` is false, create a pool of 4 `new Audio("/audio/keypress.mp3")` elements (with `volume = 0.35`, `preload = "auto"`), call `.play()` then immediately `.pause()` + `.currentTime = 0` on one of them to satisfy the user-gesture requirement, then mark `audioUnlocked = true`. Use `await` inside a `try/catch` to swallow any NotAllowedError.
       - Call `initSound()` from the existing DOMContentLoaded handler (next to `initTheme()` at main.js:6).
    3. Add `playKey()` function at module scope:
       - If `!soundEnabled || !audioUnlocked || !keyAudio` → return.
       - Throttle: if `performance.now() - lastPlay < KEY_THROTTLE_MS` → return.
       - Pick next audio element from the 4-element pool (round-robin via a counter), reset `currentTime = 0`, call `.play().catch(() => {})` to swallow autoplay rejections silently.
       - Update `lastPlay = performance.now()`.
    4. Patch BOTH `typeCmd()` functions — one in the skills terminal around line 308, one in the experience terminal around line 433. Inside each `tick()` function, AFTER the `i++` and BEFORE `typingEl.textContent = text.slice(0, i);`, call `playKey();`. Do not play on the final empty increment (guard: only call when the new character is non-whitespace, i.e., `if (text[i-1] && text[i-1].trim()) playKey();`).
    5. Do not break existing typing timing — `playKey()` must be non-blocking (fire-and-forget `.play()`).
  </action>
  <verify>
    <automated>grep -q "function initSound" app/portfolio/frontend/public/js/main.js && grep -q "function playKey" app/portfolio/frontend/public/js/main.js && grep -q 'localStorage.*sound-enabled' app/portfolio/frontend/public/js/main.js && [ "$(grep -c 'playKey()' app/portfolio/frontend/public/js/main.js)" -ge 2 ] && grep -q "KEY_THROTTLE_MS" app/portfolio/frontend/public/js/main.js && node -e "new Function(require('fs').readFileSync('app/portfolio/frontend/public/js/main.js','utf8'))" </automated>
  </verify>
  <done>
    - initSound() wired into DOMContentLoaded, default OFF, persists to localStorage.
    - First click unlocks audio via user gesture.
    - playKey() called inside both typeCmd tick() loops, throttled to KEY_THROTTLE_MS.
    - main.js parses cleanly (node -e Function constructor check passes).
    - Manual smoke test (optional, not automated): `cd app/portfolio/frontend && node server.js`, open http://localhost:3000, scroll to skills section → verify silence. Click sound toggle → verify typing chars now emit clicks. Reload → verify preference persists and audio unlocks on first toggle click again (new session = new gesture requirement).
  </done>
</task>

</tasks>

<verification>
Overall checks after all three tasks:
1. `grep -q "mediaSrc" app/portfolio/frontend/server.js` — CSP updated.
2. `test -f app/portfolio/frontend/public/audio/keypress.mp3` — asset bundled.
3. `grep -q 'id="soundToggle"' app/portfolio/frontend/public/index.html` — button present.
4. `grep -q "function initSound" app/portfolio/frontend/public/js/main.js && grep -c 'playKey()' app/portfolio/frontend/public/js/main.js | awk '$1 >= 2'` — sound hooked into both terminals.
5. `node -e "new Function(require('fs').readFileSync('app/portfolio/frontend/public/js/main.js','utf8'))"` — JS parses.
6. Manual: run frontend locally, confirm default-off, toggle persists, typing emits sound.
</verification>

<success_criteria>
- Sound toggle button visible in nav, matches theme-toggle styling idiom.
- Default state is OFF; `localStorage["sound-enabled"]` initially absent or "false".
- After clicking toggle ON, subsequent typewriter characters (skills + experience terminals) emit the keypress sound without clipping.
- Reload preserves preference; first click after reload re-unlocks audio via user gesture.
- No console errors, no CSP violations, no autoplay-blocked warnings in default-OFF state.
</success_criteria>

<output>
After completion, create `.planning/quick/260415-q8z-add-terminal-typing-asmr-sound-synced-to/260415-q8z-SUMMARY.md` per the summary template.
</output>
