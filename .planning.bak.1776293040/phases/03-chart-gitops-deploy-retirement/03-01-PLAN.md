---
phase: 03-chart-gitops-deploy-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HelmCharts/portfolio/values.yaml
  - HelmCharts/portfolio/templates/01-backend.yaml
  - HelmCharts/portfolio/templates/00-serviceaccounts.yaml
autonomous: true
requirements: [DEP-01, DEP-05, SEC-01, SEC-03, SEC-04]
must_haves:
  truths:
    - "HelmCharts/portfolio/values.yaml exists with sane defaults for namespace, replicas, images (web/api keys), ports (api=5000, web=3000), resources, api.url, smtp/config env"
    - "portfolio-api Deployment template targets containerPort 5000 and both probes hit GET /health on 5000"
    - "portfolio-api container has envFrom.secretRef.name: portfolio-smtp AND explicit env entries for ALLOWED_ORIGINS, RATE_LIMIT, RATE_WINDOW_MINUTES, MAX_BODY_BYTES, BACKEND_PORT"
    - "Dedicated ServiceAccount portfolio-api exists and is referenced by the api Deployment (spec.template.spec.serviceAccountName)"
    - "Chart value key rename complete: images.api / ports.api / resources.api retained; images.frontend renamed to images.web (this plan covers api side; values.yaml also defines images.web used by Plan 03-02)"
    - "`helm template HelmCharts/portfolio` renders without nil-ref errors using values.yaml defaults"
  artifacts:
    - path: "HelmCharts/portfolio/values.yaml"
      provides: "Default values for chart rendering + helm lint"
      contains: "api: 5000"
    - path: "HelmCharts/portfolio/templates/01-backend.yaml"
      provides: "portfolio-api Deployment + Service at port 5000 with envFrom portfolio-smtp and probes on /health"
      contains: "envFrom"
    - path: "HelmCharts/portfolio/templates/00-serviceaccounts.yaml"
      provides: "ServiceAccounts portfolio-api and portfolio-web for Istio mTLS identity (D-10)"
      contains: "kind: ServiceAccount"
  key_links:
    - from: "portfolio-api Deployment"
      to: "Secret portfolio-smtp (materialized from SealedSecret)"
      via: "envFrom.secretRef"
      pattern: "secretRef:\\s*\\n\\s*name:\\s*portfolio-smtp"
    - from: "portfolio-api Deployment"
      to: "ServiceAccount portfolio-api"
      via: "spec.template.spec.serviceAccountName"
      pattern: "serviceAccountName:\\s*portfolio-api"
---

<objective>
Create the chart's `values.yaml` (currently missing — Phase 2 VERIFICATION noted `helm template` fails on nil refs) and update the backend Deployment template to (a) run on port 5000, (b) probe GET /health, (c) consume SMTP creds via `envFrom.secretRef: portfolio-smtp`, (d) receive explicit non-secret env vars, (e) bind to a dedicated `portfolio-api` ServiceAccount.

Purpose: Without a sane default `values.yaml`, `helm lint` and the chart-publish workflow (Plan 03-04) cannot run. Without port 5000 alignment, the pod fails readiness. Without envFrom, SMTP is unwired and Phase 4 cannot send email. Without a dedicated SA, the Istio AuthorizationPolicy in Plan 03-03 cannot key on principals.

Output: Three files — values.yaml, updated 01-backend.yaml, and a new 00-serviceaccounts.yaml that also defines portfolio-web's SA (so Plan 03-02 can reference it without file contention; file is named 00- to render first).

Honors D-01 (port 5000), D-02 (key rename web/api + image annotations — this plan wires values.yaml image keys), D-03 (/health probe), D-04 (envFrom + explicit env), D-05 (CORS origins), D-09 (resources unchanged), D-10 (ServiceAccounts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-chart-gitops-deploy-retirement/03-CONTEXT.md
@.planning/phases/01-package-local-verify/01-SUMMARY.md
@.planning/phases/02-secrets-ci-image-push/02-SUMMARY.md
@HelmCharts/portfolio/Chart.yaml
@HelmCharts/portfolio/templates/01-backend.yaml
@HelmCharts/portfolio/templates/sealed-secret.yaml

<interfaces>
Existing backend template variables referenced:
  .Values.namespace.name
  .Values.replicas.api
  .Values.images.api
  .Values.ports.api
  .Values.resources.api.{requests,limits}.{cpu,memory}

Secret consumed (from Phase 2):
  kind: Secret
  name: portfolio-smtp
  namespace: portfolio
  keys: SMTP_USER, SMTP_PASS, RECIPIENT_EMAIL
  source: templates/sealed-secret.yaml (SealedSecret ciphertext, decrypted in-cluster)

Backend /health endpoint (Phase 1): GET /health → 200 (non-proxied; also /api/health exists)
Backend listens on 5000 (Phase 1 Dockerfile EXPOSE 5000, BACKEND_PORT default 5000)

Backend env vars read by app/portfolio/backend/app.py (APP-03):
  SMTP_HOST, SMTP_PORT, SMTP_USER (secret), SMTP_PASS (secret), RECIPIENT_EMAIL (secret)
  ALLOWED_ORIGINS, RATE_LIMIT, RATE_WINDOW_MINUTES, MAX_BODY_BYTES, BACKEND_PORT

Chart value-key rename (D-02):
  OLD: .Values.images.frontend, .Values.ports.frontend, .Values.replicas.frontend, .Values.resources.frontend, .Values.api.url
  NEW: .Values.images.web,      .Values.ports.web,      .Values.replicas.web,      .Values.resources.web,      .Values.api.url (kept)
  api keys stay as .Values.{images,ports,replicas,resources}.api

Image-automation annotation format (from image-automation comments must survive in values.yaml so Flux can rewrite the tag):
  web: 372517046622.dkr.ecr.us-east-1.amazonaws.com/portfolio-web:PLACEHOLDER # {"$imagepolicy": "flux-system:portfolio-web"}
  api: 372517046622.dkr.ecr.us-east-1.amazonaws.com/portfolio-api:PLACEHOLDER # {"$imagepolicy": "flux-system:portfolio-api"}
(Note: the HelmRelease inline values also carry these annotations — Plan 03-03. The chart defaults are a safety net; the HelmRelease wins.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HelmCharts/portfolio/values.yaml with renamed keys and image-automation markers (D-01, D-02, D-05, D-09)</name>
  <files>HelmCharts/portfolio/values.yaml</files>
  <action>
Create new file. Use the EXACT structure below. Note the `# {"$imagepolicy": ...}` comments — they MUST be preserved on the image lines; Flux image-automation (installed in Phase 2, plan 02-04) matches those lines by text pattern. The placeholder SHA `PLACEHOLDER` will be overwritten automatically when image-automation first runs.

```yaml
# Default values for the portfolio chart.
# The HelmRelease in portfolio/base/helmrelease.yaml overrides these;
# these defaults exist so `helm lint` and `helm template` succeed
# and so the chart is usable standalone.
namespace:
  name: portfolio

replicas:
  api: 2
  web: 2

images:
  # Flux image-automation rewrites the tag after these lines (D-02, Phase 2 D-04).
  web: 372517046622.dkr.ecr.us-east-1.amazonaws.com/portfolio-web:PLACEHOLDER # {"$imagepolicy": "flux-system:portfolio-web"}
  api: 372517046622.dkr.ecr.us-east-1.amazonaws.com/portfolio-api:PLACEHOLDER # {"$imagepolicy": "flux-system:portfolio-api"}

ports:
  api: 5000
  web: 3000

api:
  # In-cluster URL used by the frontend container (D-04).
  url: "http://portfolio-api.portfolio.svc.cluster.local:5000"

# Non-secret backend config (D-04, D-05). SMTP_* + RECIPIENT_EMAIL come from the portfolio-smtp Secret via envFrom.
config:
  allowedOrigins: "https://yedressov.com,http://localhost:3000"
  rateLimit: "5"
  rateWindowMinutes: "15"
  maxBodyBytes: "16384"

resources:
  api:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi
  web:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

serviceAccount:
  # D-10 — dedicated SAs so the AuthorizationPolicy in Plan 03-03 can key on mTLS principals.
  api: portfolio-api
  web: portfolio-web
```

Why these defaults (not "v1"): they are the full production values the HelmRelease will also carry (D-04, D-05). The chart is a real unit on its own.
  </action>
  <verify>
    <automated>test -f HelmCharts/portfolio/values.yaml && grep -q 'api: 5000' HelmCharts/portfolio/values.yaml && grep -q '\$imagepolicy' HelmCharts/portfolio/values.yaml && grep -q 'allowedOrigins: "https://yedressov.com' HelmCharts/portfolio/values.yaml</automated>
  </verify>
  <done>values.yaml exists with ports.api=5000, renamed web keys, image-automation markers, CORS origins, and SA names.</done>
</task>

<task type="auto">
  <name>Task 2: Create HelmCharts/portfolio/templates/00-serviceaccounts.yaml (D-10)</name>
  <files>HelmCharts/portfolio/templates/00-serviceaccounts.yaml</files>
  <action>
Create new file. Defines both SAs so the AuthorizationPolicy in Plan 03-03 can reference stable principals `cluster.local/ns/portfolio/sa/portfolio-api` and `.../sa/portfolio-web`. File prefix `00-` makes it render before the Deployments (Helm renders alphabetically within templates/).

```yaml
# ── ServiceAccounts (D-10) ──────────────────────────────────────────────────
# Dedicated identities so Istio mTLS principals are distinct per workload.
# No IRSA annotations — pods do not call AWS APIs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.api | default "portfolio-api" }}
  namespace: {{ .Values.namespace.name }}
  labels:
    app: portfolio-api
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.web | default "portfolio-web" }}
  namespace: {{ .Values.namespace.name }}
  labels:
    app: portfolio-web
```
  </action>
  <verify>
    <automated>helm template HelmCharts/portfolio 2>/dev/null | grep -A2 'kind: ServiceAccount' | grep -q 'portfolio-api' && helm template HelmCharts/portfolio 2>/dev/null | grep -A2 'kind: ServiceAccount' | grep -q 'portfolio-web'</automated>
  </verify>
  <done>Two ServiceAccounts render via `helm template`.</done>
</task>

<task type="auto">
  <name>Task 3: Update HelmCharts/portfolio/templates/01-backend.yaml (D-01, D-03, D-04, D-10)</name>
  <files>HelmCharts/portfolio/templates/01-backend.yaml</files>
  <action>
Rewrite the file. Preserve the existing strong pod+container securityContext (runAsNonRoot, readOnlyRootFilesystem, dropped caps, seccomp RuntimeDefault — per SEC-04) verbatim. Change exactly these things:

1. Under `spec.template.spec`, add `serviceAccountName: {{ .Values.serviceAccount.api | default "portfolio-api" }}` above `securityContext:` (D-10).

2. Container `ports[0].containerPort` — already `{{ .Values.ports.api }}`, now resolves to 5000 via values.yaml (D-01).

3. Both probes — change `path: /api/health` → `path: /health` (D-03). Keep the probes otherwise as-is.

4. After the `ports:` block (before `resources:`), add:
```yaml
          envFrom:
            - secretRef:
                name: portfolio-smtp
          env:
            - name: BACKEND_PORT
              value: {{ .Values.ports.api | quote }}
            - name: ALLOWED_ORIGINS
              value: {{ .Values.config.allowedOrigins | quote }}
            - name: RATE_LIMIT
              value: {{ .Values.config.rateLimit | quote }}
            - name: RATE_WINDOW_MINUTES
              value: {{ .Values.config.rateWindowMinutes | quote }}
            - name: MAX_BODY_BYTES
              value: {{ .Values.config.maxBodyBytes | quote }}
```

Rationale for envFrom-before-env: if both lists define the same key, the later `env:` wins (Kubernetes semantics). We keep secret keys ONLY in envFrom, and non-secret config ONLY in env — no overlap, but the order also ensures any future overlap prefers explicit env (safer for local overrides).

5. Leave the Service block (port 5000 via `{{ .Values.ports.api }}`) intact — already correctly templated.

6. Volume `tmp` stays — Phase 1 Dockerfile with readOnlyRootFilesystem needs writable /tmp.

Do NOT remove the existing readOnlyRootFilesystem / drop ALL / seccompProfile settings — those satisfy SEC-04 / Kyverno PSS-Restricted.

Full file after edits (reference — exact whitespace preserved):

```yaml
# ── Backend Deployment ───────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app: portfolio-api
    tier: backend
spec:
  replicas: {{ .Values.replicas.api }}
  selector:
    matchLabels:
      app: portfolio-api
  template:
    metadata:
      labels:
        app: portfolio-api
        tier: backend
    spec:
      serviceAccountName: {{ .Values.serviceAccount.api | default "portfolio-api" }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: {{ .Values.images.api }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: {{ .Values.ports.api }}
          envFrom:
            - secretRef:
                name: portfolio-smtp
          env:
            - name: BACKEND_PORT
              value: {{ .Values.ports.api | quote }}
            - name: ALLOWED_ORIGINS
              value: {{ .Values.config.allowedOrigins | quote }}
            - name: RATE_LIMIT
              value: {{ .Values.config.rateLimit | quote }}
            - name: RATE_WINDOW_MINUTES
              value: {{ .Values.config.rateWindowMinutes | quote }}
            - name: MAX_BODY_BYTES
              value: {{ .Values.config.maxBodyBytes | quote }}
          resources:
            requests:
              cpu: {{ .Values.resources.api.requests.cpu }}
              memory: {{ .Values.resources.api.requests.memory }}
            limits:
              cpu: {{ .Values.resources.api.limits.cpu }}
              memory: {{ .Values.resources.api.limits.memory }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.ports.api }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.ports.api }}
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 100Mi
---
# ── Backend Service (ClusterIP — internal only) ─────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: portfolio-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app: portfolio-api
spec:
  type: ClusterIP
  selector:
    app: portfolio-api
  ports:
    - port: {{ .Values.ports.api }}
      targetPort: {{ .Values.ports.api }}
      protocol: TCP
```
  </action>
  <verify>
    <automated>helm lint HelmCharts/portfolio && helm template HelmCharts/portfolio | awk '/kind: Deployment/,/^---$/' | grep -q 'serviceAccountName: portfolio-api' && helm template HelmCharts/portfolio | grep -q 'containerPort: 5000' && helm template HelmCharts/portfolio | grep -q 'path: /health' && helm template HelmCharts/portfolio | grep -q 'name: portfolio-smtp' && helm template HelmCharts/portfolio | grep -q 'ALLOWED_ORIGINS' && ! helm template HelmCharts/portfolio | grep -q 'path: /api/health'</automated>
  </verify>
  <done>helm lint passes; rendered backend Deployment uses SA portfolio-api, port 5000, probes /health, mounts portfolio-smtp via envFrom, and exposes the 5 non-secret env vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cluster external → istio-ingress | Untrusted traffic terminates here (out of scope this plan) |
| istio-ingress → portfolio-api | In-mesh but crosses namespace trust boundary |
| portfolio-web → portfolio-api | In-mesh pod-to-pod (locked by AuthorizationPolicy in Plan 03-03) |
| portfolio-api → Secret portfolio-smtp | Secret material enters pod env; must never appear in logs or chart values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | values.yaml | mitigate | Secrets live in portfolio-smtp Secret via envFrom only; values.yaml carries no SMTP_* keys |
| T-03-02 | Tampering | portfolio-api pod filesystem | mitigate | readOnlyRootFilesystem + dropped caps + non-root uid 1001 (preserved from existing template, SEC-04) |
| T-03-03 | Elevation of Privilege | portfolio-api container | mitigate | allowPrivilegeEscalation: false, seccompProfile RuntimeDefault |
| T-03-04 | Spoofing | pod identity vs Istio mTLS | mitigate | Dedicated ServiceAccount portfolio-api issues a distinct SPIFFE principal for AuthorizationPolicy (Plan 03-03) |
| T-03-05 | Denial of Service | /health probe vs app work | accept | /health is cheap (returns static 200), no amplification risk |
| T-03-06 | Information Disclosure | env var logging | accept | Flask/gunicorn do not echo env to logs by default; covered in app review Phase 1 |
</threat_model>

<verification>
- `helm lint HelmCharts/portfolio` passes with zero errors.
- `helm template HelmCharts/portfolio` renders without nil-ref errors (resolves Phase 2 VERIFICATION finding).
- Rendered backend Deployment has: serviceAccountName portfolio-api, containerPort 5000, probes on /health:5000, envFrom secretRef portfolio-smtp, explicit env for ALLOWED_ORIGINS + RATE_LIMIT + RATE_WINDOW_MINUTES + MAX_BODY_BYTES + BACKEND_PORT.
- `grep -R "SMTP_PASS" HelmCharts/portfolio/values.yaml` returns zero hits (no plaintext secret in defaults).
- Preserved securityContext block passes Kyverno PSS-Restricted (SEC-04).
</verification>

<success_criteria>
Maps to ROADMAP Phase 3 success criteria #1 (probes + resources + PSS-Restricted securityContext) and #2 (envFrom.secretRef with no credentials in chart values). Requirements: DEP-01, DEP-05, SEC-01 (CORS origins in values), SEC-03 (Secret via envFrom), SEC-04 (hardened securityContext).
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-gitops-deploy-retirement/03-01-SUMMARY.md`. Note that Plan 03-02 will add the portfolio-web Deployment/Service against the same values.yaml keys (ports.web, images.web, resources.web, api.url).
</output>
