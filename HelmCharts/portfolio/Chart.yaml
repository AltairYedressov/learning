apiVersion: v2
name: portfolio
description: A Helm chart for Kubernetes
# Phase 3 cutover (2026-04): rename frontend->web, backend port 8000->5000,
# sealed-secret envFrom, dedicated ServiceAccounts, Istio AuthorizationPolicy.
type: application

# Chart version — bumped on every meaningful change; ECR enforces immutable
# versions. Phase 3 minor bump signals breaking value-key rename + port change.
version: 0.2.0

# appVersion mirrors chart version at cutover; image-automation rewrites the
# actual running image tags independently.
appVersion: "0.2.0"
